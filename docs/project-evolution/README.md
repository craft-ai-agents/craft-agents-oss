# Project Evolution Archive

This folder contains historical project documentation retained for **legal reasons**. Do not delete files here without consulting legal/leadership.

## Operational notes

- **Excluded from the OSS mirror** by allow-list (this folder is not listed in `scripts/oss-allow-list.txt`). Do not add it.
- **Avoid Windows-illegal characters** (`< > : " | ? *`) in filenames. The CI guard in `.github/workflows/validate.yml` rejects them. `Hermes : Anna thinking.pdf` was renamed to `Hermes - Anna thinking.pdf` for this reason — original filename is preserved here for record-keeping.
