{
  "version": "1.0",
  "description": "Default configuration values for G4 OS",
  "defaults": {
    "authType": "api_key",
    "notificationsEnabled": true,
    "colorTheme": "default"
  },
  "workspaceDefaults": {
    "thinkingLevel": "think",
    "permissionMode": "safe",
    "cyclablePermissionModes": ["safe", "allow-all"],
    "localMcpServers": {
      "enabled": true
    }
  }
}
