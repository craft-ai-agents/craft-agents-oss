# Coding Style Rules

- Prefer TypeScript and functional React components.
- Avoid mutation; create new objects and arrays.
- Handle errors explicitly.
- Keep functions small and focused.
- Avoid console.log in production paths (use structured logging when available).
