# Rewrite /s/assets/* to /assets/* (must be before SPA fallback)
/s/assets/*  /assets/:splat  200

# SPA routing for viewer UI. /s/api/* is handled by Pages Functions (no rewrite).
/s/*  /index.html  200
