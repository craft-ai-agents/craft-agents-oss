---
name: procurement-supplier-shortlist
description: 查飞书 Base 里的供应商档案(同步到本地缓存),按品牌、品类、供应商类型整理出候选名单。当用户问"哪些供应商能供这个/帮我列几家供应商/谁家做这个品类"时使用。只产出供应商名单,不查报价不查库存。
metadata:
  short-description: 供应商候选名单筛选
  lang: zh
---

# 供应商候选名单筛选

输入品牌/品类(可带型号特征),查飞书 Base 同步到本地的供应商档案缓存,整理采购可联系的候选名单。**只读,不写表。**

不做供应商真假、付款风险、价格优劣、下单判断。

## 怎么查:一条命令

供应商档案已由 `feishu-db` 工具从飞书 bulk-sync 进本地 SQLite。一条命令按品牌/品类匹配(跨主营/优势/询价品牌),**瞬间返回、无飞书限流**:

    .agents/skills/feishu-db/bin/feishu-db supplier --brand <品牌或品类>

返回 JSON `rows[]`:每条含 `supplier`(供应商全称)`/ type`(供应商类型)`/ grade`(等级)`/ main_brands`(主营品牌)`/ advantage`(优势产品)`/ contact`(联系方式)`/ media`(联系媒介)`/ site`(官网\|店铺)`/ status`(供应商状态)`/ note`(备注)`/ score`(总分)。

- **品牌中英文互查**:用户给中文品牌(如"欧姆龙"),也用英文("Omron")再查一次,反之亦然。两次结果合并去重。对照表见 AGENTS.md「品牌中英文对照」。
- **按品类**:品类词(如"继电器""连接器")直接当 `--brand` 传——优势产品字段里也会命中。

## 筛选原则

- 优先:主营/优势品牌命中、原厂/授权代理/授权分销商/平台自营、已建联、联系方式明确。
- **匹配原因看命中强弱**:`main_brands` 命中(强)> `advantage` 提及(中)> 仅历史线索(弱)。结果里 `main_brands` 含目标品牌的是强匹配。
- **供应商类型只看 `type` 字段**(如"授权代理/授权分销商""贸易商"),不拿 `note`/付款风险文字推断类型。贸易商可入候选,但必须标明类型,不伪装授权/原厂。
- **联系方式 vs 联系媒介**:有具体 `contact` 才写"联系方式可用";只有 `media`(QQ/微信/电话渠道)时只能写"有联系媒介记录"。
- **短品牌名防子串误命中**(`ST` 不等于 Kingston/Transcend/storage;`TI` 易误中)——核对 `main_brands` 是否真含该品牌,不要被 `advantage`/`note` 里的子串带偏。

## 输出

候选列表:供应商全称、类型/等级、匹配原因(强/弱,注明命中字段)、联系方式或联系媒介、官网\|店铺、供应商状态、备注;未覆盖项;人工确认项(是否仍合作/可供该型号/有无现货报价/需否补授权)。

> JSON 字段挑上面这些给采购,别整段照搬。

## 兜底:缓存不可用时才走 live

仅当命令报错(二进制缺失)或 `rows` 空且疑似缓存未同步时,退回直接查飞书(子命令带 `+` 前缀;中英文两次查询串行、不并发):

    lark-cli base +record-search --as user --base-token Mjlkb49B9aoptssVw8Jc0wGwnhh --table-id tblbtuMHFIOr6Oss --json '{"keyword":"<品牌>","search_fields":["主营品牌","优势产品","询价品牌"]}'

`--as user` 下若返回 `91403 permission`/登录失效,如实告诉用户去开 base 读权限 / 重登 lark-cli,不要编造,不要反复切身份重试。

## 边界

候选 ≠ 准入。有候选 ≠ 有库存。付款风险字段只展示,不在本 skill 做风险判断。只读不写。
