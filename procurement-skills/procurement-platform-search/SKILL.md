---
name: procurement-platform-search
description: 采购平台报价/库存业务入口。当本地库存无记录后需要继续查外部平台，或用户明确要求查平台报价、哪里有货、市场价、交期、外部渠道可得性时使用。取得结构化平台证据，并整理为采购可读的库存、价格、交期、型号一致性和证据链接。
---

# 平台报价查找

本 skill 负责采购业务层的平台结果整理。这里按采购需要处理采集层返回的结构化证据。

## 工作流

1. 确认入口：本地库存已经查完为空，或用户明确要求继续查外部平台。
2. 保留用户原始型号：不要先合并后缀、包装、温度等级或版本号。
3. 第一次查询必须全平台查找：准确度优先，首轮不要只查少数平台，不要因为耗时提前缩小范围；后续只针对缺口、异常或型号差异做定向复核。
4. 取得结构化平台证据：只消费返回的型号、平台、品牌、描述、库存、价格、MOQ、交期、链接和错误状态。
5. 做型号一致性核对：平台返回型号与用户型号不完全一致时，逐字符写清差异，例如多了包装后缀、少了温度等级、版本字母不同。
6. 合并同一平台的重复行：同平台同型号保留库存/价格最完整的行，证据链接不同但信息互补时可以合并到同一行。
7. 区分状态：有结果、正常无匹配、本次未取到要分开写；访问受限或报错只能说本次未取到，不能说成无货。

## 输出

输出给采购看平台证据，不输出采集过程。没有上层指定格式时，用下面的结构：

```text
结论：平台查到 X 条可核对结果；Y 个平台未返回匹配；Z 个平台本次未取到。

平台结果：
| 平台 | 返回型号 | 型号一致性 | 品牌/描述 | 库存 | 价格/MOQ | 交期 | 证据 | 状态/需确认 |
|---|---|---|---|---|---|---|---|---|
| ... | ... | 完全一致 / 差异：... | ... | ... | ... | ... | ... | ... |

未返回匹配/本次未取到：
- <平台>：未返回匹配 / 本次未取到（原因：...）
```

写法要求：

- `结论` 只概括首轮全平台覆盖结果，不写采购建议。
- `型号一致性` 必须具体；不要只写“需复核”。
- `库存`、`价格/MOQ`、`交期` 没有证据就写“未返回”，不要补猜。
- `证据` 优先给产品页或 datasheet 链接；没有链接就写来源字段。
- 如果上层要求 JSON、表单字段或批量汇总格式，按上层格式映射这些字段，不套用本表格。

## 反合理化表格

| 常见借口 | 反驳 |
|---|---|
| “平台没返回结果，就是无货。” | 正常无匹配和本次未取到要分开；访问受限不是无货证据。 |
| “型号差一个后缀也算同一个。” | 后缀可能代表包装、温度、速度、版本或封装，必须逐字符说明差异。 |
| “看到价格最低的一行，直接推荐给采购。” | 本 skill 只汇总平台证据；不做供应商准入、下单建议或性价比排序。 |
| “WebSearch 看到库存页，也能报库存价格。” | 库存、价格、可得性只认结构化平台证据；网页搜索只能辅助识别型号信息。 |
| “平台返回 similar/alternate，就能当替代料。” | 平台相似项只是线索，不能在这里下可替代结论。 |
